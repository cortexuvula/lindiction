# Rust
/target/
**/*.rs.bk
Cargo.lock.orig

# Whisper models (large, downloaded separately)
/models/*.bin
/models/*.gguf

# Claude session state
/.remember/

# Editor / OS
.vscode/
.idea/
*.swp
.DS_Store

# Build artifacts (local `cargo deb` output — release artifacts are built by CI)
*.deb
*.deb.sha256
